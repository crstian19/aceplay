name: AUR Publish

on:
  push:
    tags: [ 'v*' ]

permissions:
  contents: write

jobs:
  aur-publish:
    name: Publish to AUR
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        ref: ${{ github.sha }}
        fetch-depth: 0

    - name: Get tag version
      run: |
        VERSION="${GITHUB_REF#refs/tags/v}"
        echo "VERSION=$VERSION" >> $GITHUB_ENV

    - name: Debug
      run: |
        echo "VERSION: $VERSION"
        ls -la scripts/

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26'

    - name: Build
      run: |
        go build -ldflags "-X main.version=$VERSION" -o aceplay ./cmd

    - name: Configure Git
      run: |
        git config --global user.name "${{ secrets.AUR_USERNAME }}"
        git config --global user.email "${{ secrets.AUR_EMAIL }}"

    - name: Prepare aceplay-bin
      run: |
        mkdir -p /tmp/aceplay-bin
        cp scripts/PKGBUILD.bin /tmp/aceplay-bin/PKGBUILD
        cp scripts/.SRCINFO.bin /tmp/aceplay-bin/.SRCINFO
        cp scripts/aceplay.desktop /tmp/aceplay-bin/
        cp scripts/LICENSE /tmp/aceplay-bin/
        sed -i "s/pkgver=.*/pkgver=$VERSION/" /tmp/aceplay-bin/PKGBUILD
        sed -i "s/__VERSION__/$VERSION/g" /tmp/aceplay-bin/.SRCINFO
        echo "=== PKGBUILD ==="
        cat /tmp/aceplay-bin/PKGBUILD
        echo "=== .SRCINFO ==="
        cat /tmp/aceplay-bin/.SRCINFO

    - name: Publish aceplay-bin to AUR
      env:
        AUR_SSH_KEY: ${{ secrets.AUR_SSH_KEY }}
        AUR_USERNAME: ${{ secrets.AUR_USERNAME }}
      run: |
        set -x
        mkdir -p ~/.ssh
        echo "$AUR_SSH_KEY" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        chmod 700 ~/.ssh
        ssh-keyscan -H aur.archlinux.org >> ~/.ssh/known_hosts 2>/dev/null || true
        
        if [ -d "/tmp/aceplay-bin-repo" ]; then
          rm -rf /tmp/aceplay-bin-repo
        fi
        echo "Cloning aceplay-bin repo..."
        GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no -v" git clone aur@aur.archlinux.org:aceplay-bin.git /tmp/aceplay-bin-repo
        cd /tmp/aceplay-bin-repo
        ls -la
        cp /tmp/aceplay-bin/* .
        cp /tmp/aceplay-bin/.SRCINFO .
        git add -A
        git add -f .SRCINFO
        git status
        if git diff --staged --quiet; then
          echo "No changes to commit for aceplay-bin"
        else
          echo "Committing and pushing..."
          git commit -m "Update to v$VERSION"
          GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no" git push origin master
        fi

    - name: Prepare aceplay
      run: |
        mkdir -p /tmp/aceplay
        cp scripts/PKGBUILD /tmp/aceplay/PKGBUILD
        cp scripts/.SRCINFO /tmp/aceplay/.SRCINFO
        cp scripts/aceplay.desktop /tmp/aceplay/
        cp scripts/LICENSE /tmp/aceplay/
        sed -i "s/pkgver=.*/pkgver=$VERSION/" /tmp/aceplay/PKGBUILD
        sed -i "s/__VERSION__/$VERSION/g" /tmp/aceplay/.SRCINFO
        echo "=== PKGBUILD ==="
        cat /tmp/aceplay/PKGBUILD
        echo "=== .SRCINFO ==="
        cat /tmp/aceplay/.SRCINFO

    - name: Publish aceplay to AUR
      env:
        AUR_SSH_KEY: ${{ secrets.AUR_SSH_KEY }}
        AUR_USERNAME: ${{ secrets.AUR_USERNAME }}
      run: |
        set -x
        if [ -d "/tmp/aceplay-repo" ]; then
          rm -rf /tmp/aceplay-repo
        fi
        echo "Cloning aceplay repo..."
        GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no -v" git clone aur@aur.archlinux.org:aceplay.git /tmp/aceplay-repo
        cd /tmp/aceplay-repo
        ls -la
        cp /tmp/aceplay/* .
        cp /tmp/aceplay/.SRCINFO .
        git add -A
        git add -f .SRCINFO
        git status
        if git diff --staged --quiet; then
          echo "No changes to commit for aceplay"
        else
          echo "Committing and pushing..."
          git commit -m "Update to v$VERSION"
          GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no" git push origin master
        fi
